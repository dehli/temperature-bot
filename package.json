{
  "dependencies": {
    "@tsconfig/node16": "^16.1.1",
    "@types/node": "^20.10.3",
    "aws-cdk-lib": "^2.113.0",
    "typescript": "^5.3.2"
  },
  "scripts": {
    "build": "tsc -p ./",
    "clean": "rm -rf out",
    "deploy": "npx cdk deploy",
    "predeploy": "npm run clean && npm run build"
  },
  "devDependencies": {
    "prettier": "3.1.0"
  }
}
