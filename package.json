{
  "dependencies": {
    "@tsconfig/node14": "^1.0.0",
    "aws-cdk": "^2.8.0",
    "monocdk": "^1.139.0",
    "typescript": "^4.5.4"
  },
  "scripts": {
    "build": "tsc -p ./",
    "clean": "rm -rf out",
    "deploy": "npx cdk deploy",
    "predeploy": "npm run clean && npm run build"
  }
}
